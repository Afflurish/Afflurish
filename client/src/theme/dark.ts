import { CustomFlowbiteTheme } from 'flowbite-react';

const darkTheme: CustomFlowbiteTheme = {
    accordion: {
        root: {
            base: "bg-red-500"
        }
    }
};

export default darkTheme;